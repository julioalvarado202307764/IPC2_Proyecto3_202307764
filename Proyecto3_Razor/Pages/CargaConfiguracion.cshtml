@page
@model Proyecto3_Razor.Pages.CargaConfiguracionModel
@{
    ViewData["Title"] = "Cargar Configuración";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Cargar Archivo de Configuración</h4>
                </div>
                <div class="card-body">
                    <form method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="archivo" class="form-label">Seleccione el archivo config.xml:</label>
                            <input class="form-control" type="file" id="archivo" asp-for="ArchivoXml" accept=".xml" required>
                        </div>
                        <button type="submit" class="btn btn-success w-100">
                            <i class="bi bi-cloud-upload"></i> Procesar y Cargar Datos
                        </button>
                    </form>

                    @if (!string.IsNullOrEmpty(Model.MensajeRespuesta))
                    {
                        <div class="alert alert-info mt-4 text-break shadow-sm">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <h5 class="mb-0">Respuesta del Servidor:</h5>
                                
                                <!-- 1. BOTÓN DE DESCARGA -->
                                <button type="button" onclick="descargarRespuestaXML()" class="btn btn-primary btn-sm fw-bold">
                                    <i class="bi bi-download"></i> Descargar XML
                                </button>
                            </div>
                            
                            <!-- 2. LE PUSIMOS EL ID 'texto-xml-respuesta' AL PRE -->
                            <pre id="texto-xml-respuesta" class="bg-white p-3 border rounded mb-0">@Model.MensajeRespuesta</pre>
                        </div>
                        <script>
                            function descargarRespuestaXML() {
                                // Obtenemos el texto
                                const contenidoXml = document.getElementById('texto-xml-respuesta').innerText;
                                
                                // Creamos el archivo en memoria
                                const blob = new Blob([contenidoXml], { type: 'application/xml' });
                                const url = window.URL.createObjectURL(blob);
                                
                                // Forzamos la descarga
                                const a = document.createElement('a');
                                a.style.display = 'none';
                                a.href = url;
                                a.download = 'Respuesta_Config_' + new Date().getTime() + '.xml'; 
                                
                                document.body.appendChild(a);
                                a.click();
                                window.URL.revokeObjectURL(url);
                                document.body.removeChild(a);
                            }
                        </script>
                    }
                </div>
            </div>
        </div>
    </div>
</div>